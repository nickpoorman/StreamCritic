/* Close the lightbox */
$(document).trigger('close.facebox')

/* Change the nav links to signed in status  */
$("#nav_menu").html("<%= escape_javascript(render(:partial => 'menu/nav_menu')) %>");

/* What page did it come from? */
if ($("#page_video_show").length >= 1) {
  /* Fix the show page */
} else if ($("#page_index_show").length >= 1) {
  /* Load the index partial if index page */
  /* $("#index_page").html("<%= escape_javascript(render(:partial => 'index')) %>"); */
  $("#social_text").append("<%= escape_javascript(render(:partial => 'videos/emb_form')) %>");
}
